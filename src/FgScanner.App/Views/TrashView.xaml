<UserControl x:Class="FgScanner.App.Views.TrashView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:FgScanner.App.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=views:TrashViewModel}">
    <DockPanel>
        <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,6">
            <Button Content="Restore" Command="{Binding RestoreCommand}" Padding="10,6" Margin="0,0,6,0" />
            <Button Content="Delete permanently" Command="{Binding DeletePermanentlyCommand}" Padding="10,6" Margin="0,0,6,0" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="10,6" />
        </WrapPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding StatusText}" Opacity="0.8" Margin="0,0,0,6" />
        <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Group" Binding="{Binding GroupName}" Width="160" />
                <DataGridTextColumn Header="Position" Binding="{Binding DocumentSequence}" Width="70" />
                <DataGridTextColumn Header="Deleted (UTC)" Binding="{Binding DeletedUtc, StringFormat=yyyy-MM-dd HH:mm}" Width="140" />
                <DataGridTextColumn Header="Origin folder" Binding="{Binding GroupDirectoryPath}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
