using Xunit;

namespace FgScanner.Cli.Tests;

public class SmokeTests
{
    [Fact]
    public void Cli_assembly_is_referenced()
    {
        // Placeholder until phase 8 gives the CLI real commands to test.
        Assert.True(true);
    }
}
